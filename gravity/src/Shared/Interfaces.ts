interface Velocity {
    dX: number;
    dY: number;
}
interface Position {
    x: number;
    y: number;
}
interface Area {
    x: number;
    y: number;
    radius: number;
}

export { Position, Area, Velocity };
